name: Release desktop application

on:
  push:
    tags: ["app-v*"]

permissions:
  contents: write

concurrency:
  group: desktop-release-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  macos-universal:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 24, cache: npm, cache-dependency-path: desktop/package-lock.json }
      - run: npm ci
        working-directory: desktop
      - run: rustup target add aarch64-apple-darwin x86_64-apple-darwin
      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          projectPath: desktop
          args: --target universal-apple-darwin
          tagName: app-v__VERSION__
          releaseName: 抖音写作 Skills v__VERSION__
          releaseDraft: false
          prerelease: false
          generateReleaseNotes: true
          assetNamePattern: Douyin-Writing-Skills_[version]_macOS_universal[ext]
          includeUpdaterJson: true
          updaterJsonKeepUniversal: true
          updaterJsonPreferNsis: true
  windows:
    needs: macos-universal
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 24, cache: npm, cache-dependency-path: desktop/package-lock.json }
      - run: npm ci
        working-directory: desktop
      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          projectPath: desktop
          tagName: app-v__VERSION__
          releaseName: 抖音写作 Skills v__VERSION__
          releaseDraft: false
          prerelease: false
          generateReleaseNotes: true
          assetNamePattern: Douyin-Writing-Skills_[version]_windows_x64[_setup][ext]
          includeUpdaterJson: true
          updaterJsonPreferNsis: true
